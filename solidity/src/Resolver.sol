pragma solidity ^0.4.21;

contract Resolver {
	function isWinner(bytes currState, uint _winner) public pure returns(bool) {
	    return true;
	}
	function resolve(bytes _previousState, bytes _currentState) public pure returns(bool) {
	    return true;
	}
}